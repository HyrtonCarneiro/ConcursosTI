{
  "name": "concursos-ti",
  "version": "1.0.0",
  "description": "App de estudos",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
